{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build",
      "type": "shell",
      "command": "cargo build",
      "group": "build",
      "options": {
        "cwd": "${workspaceFolder}/tennessee-eastman-service/service"
      }
    }
  ]
}
